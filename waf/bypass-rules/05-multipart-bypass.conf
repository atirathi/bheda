# Bypass: Multipart boundary not fully parsed by CRS
# Attack payload hidden in multipart boundary tags evades content inspection
SecRule REQUEST_HEADERS:Content-Type "@rx multipart/form-data" "id:100500,phase:1,pass,log,msg:'Multipart form data - boundary parsing may be incomplete',setvar:tx.anomaly_score=+1"
