id: waf-4
title: "WAF Bypass - Challenge 4"
category: web
difficulty: hard
cvss: 8.8
owasp: A03:2021 – Injection
endpoint: /api/waf/4
description: "WAF bypass technique: chunked transfer encoding bypass."
hints:
  - "Insert a newline character in the SQL payload."
  - "Use Unicode fullwidth characters to evade pattern matching."
  - "Split the payload across HTTP/2 frames."
flag: "BHEDA{waf_bypass_4}"
flag_location: "Database after successful SQL injection"
solution_summary: "Inject ' UNION%0ASELECT%201,flag%20FROM%20users-- using URL-encoded newline to split the payload and evade the WAF regex."
