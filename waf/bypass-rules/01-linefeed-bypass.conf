# Bypass: CRS rules do not match on bare \n (linefeed) in payloads
# Payloads with \n instead of standard encoded chars evade detection
SecRule REQUEST_BODY "@contains \n" "id:100100,phase:2,pass,log,msg:'Linefeed detected - potential WAF bypass vector',setvar:tx.anomaly_score=+1"
