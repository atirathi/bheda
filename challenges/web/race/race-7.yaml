id: race-7
title: "Race Condition - Challenge 7"
category: web
difficulty: hard
cvss: 8.8
owasp: A01:2021 – Broken Access Control
endpoint: /api/race/7
description: "Race condition in the file upload verification logic. Concurrent requests bypass the check-then-use pattern."
hints:
  - "Send multiple concurrent requests to the endpoint."
  - "The check for sufficient balance happens before the deduction."
  - "Use curl with parallel requests or a race condition tool."
flag: "BHEDA{race_condition_7}"
flag_location: "Coupon/wallet balance after race exploitation"
solution_summary: "Send 50 concurrent requests to redeem the same coupon. The race condition allows multiple redemptions before the used flag is set."
