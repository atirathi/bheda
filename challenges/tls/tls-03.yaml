id: tls-03
title: "TLS expired"
category: tls
difficulty: easy
cvss: 5.5
owasp: A02:2021 – Cryptographic Failures
endpoint: https://localhost:44303
description: "Server uses an expired TLS certificate with private key exposed via /backup."
hints:
  - "Use openssl s_client to inspect the certificate and cipher negotiation."
  - "Force specific protocols or ciphers with -ssl3, -tls1, or -cipher flags."
  - "Check for exposed files or debug endpoints on the server."
flag: "BHEDA{tls_expired}"
flag_location: "HTTPS response body on port 44303"
solution_summary: "Connect with appropriate openssl flags. For expired: openssl s_client -connect localhost:${PORT}"
