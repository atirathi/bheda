id: tls-14
title: "TLS heartbleed"
category: tls
difficulty: hard
cvss: 8.5
owasp: A02:2021 – Cryptographic Failures
endpoint: https://localhost:44314
description: "OpenSSL 1.0.1f vulnerable to Heartbleed memory leak."
hints:
  - "Use openssl s_client to inspect the certificate and cipher negotiation."
  - "Force specific protocols or ciphers with -ssl3, -tls1, or -cipher flags."
  - "Check for exposed files or debug endpoints on the server."
flag: "BHEDA{tls_heartbleed}"
flag_location: "HTTPS response body on port 44314"
solution_summary: "Connect with appropriate openssl flags. For heartbleed: openssl s_client -connect localhost:${PORT}"
